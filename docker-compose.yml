version: '3.8'

services:
  xala-stream:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: xala-stream-app
    ports:
      - "8080:8080"
    environment:
      - IS_DOCKER=true
    restart: unless-stopped